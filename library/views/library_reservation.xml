<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- List  View -->
    <record id="view_library_reservetion_list" model="ir.ui.view">
        <field name="name">library.reservation.list</field>
        <field name="model">library.reservation</field>
        <field name="arch" type="xml">
            <list editable="bottom">
                <field name="book_id"/>
                <field name="borrower_id"/>
                <field name="reservation_date"/>
                <field name="state"/>
            </list>
        </field>
    </record>

    <!-- Action View -->
    <record id="view_library_reservetion_action" model="ir.actions.act_window">
        <field name="name">Reservation</field>
        <field name="res_model">library.reservation</field>
        <field name="view_mode">list</field>
    </record>
</odoo>